# Python
.venv/
__pycache__/
*.pyc

# 生成产物（不纳入版本控制）
*.pptx
diagnostic_results/

# scratch 调试脚本（本地保留，不入远端）
scratch/

# 归档目录（本地保留，不入远端）
archive/

# 历史数据集（只保留最新的）
dataset/ddps_v4_dataset_2026091[14]_*.csv

# 根目录交付件已移到 deliverables/ 文件夹，不再在根目录生成
/DDPS_v*_Deliverable.html
