# AGENTS.md — Agent 工作准则

> 所有接手的 Agent 必读。这是硬规矩，不是建议。

## 交付件原则（不可违反）

**交付件是资产负债表，是照相机，不是现金流，不是摄像机。**

- 交付件只讲**现状**：这个系统/方案现在是什么样、能干什么、结果数字是多少。
- 交付件**不假设读者有任何前置信息**。读者只看这一个材料就能看懂，不需要知道 v1/v2/v3/v4 怎么演变过来的、不需要知道上一版哪里错了。
- 交付件**不从"为什么改、怎么修"开始**。没有人想看你的排错过程。变化追踪、根因分析、修复过程写到 docs/ 和 git commit 里，**不进交付件**。
- 交付件**不提"上一版/前一版/v4"**。它描述的是"此刻的资产负债表"——资产是什么、负债是什么、数字是多少，就这样。
- 如果你想在交付件里写"之前怎样、现在改成了怎样"——**停下，那是现金流，不是资产负债表。删掉它。**

一句话自检：**读者看完交付件，应该知道"现在是什么"，而不是"怎么走到这里的"。**

## 交付流程（不可违反）

- **每做完一轮（一次完整交付/验收），必须把最新交付件 `deliverables/*.html` 用 present 工具发出来给用户看**，不得只在回复里贴路径或一句"已生成"。
- 交付件每重新生成 / 更新一次，就要把最新版本再 present 一次。

## 仓库与产物管理（不可违反）

- **归档判据是「现役 vs 历史」，与文件大小无关**。最新的数据、模型、结果必须保持 git 跟踪——哪怕是大二进制（如物理链路 s4p、IEEE 标准数据包）。
- 只有被新版取代的**历史产物**（历史结果 / 模型 / 数据集 / 交付件）才归档到 `archive/`（本地保留、移出远端）。
- **删除任何文件前必须先问用户**；未获同意时只允许归档，不允许删除。
- 不得以「文件大」或「当前代码没引用」为由把现役数据移出跟踪。

## 代码命名（不可违反）

- **脚本 / 函数命名不带版本号**（不带 `v2 / v3 / v4 / v5 / v6 / v62 / v6.2` 之类的后缀）。
- 现役代码只保留「当前最新实现」；历史版本函数、死代码、历史脚本一律归档到 `archive/`，不得堆叠在现役文件里。
- 需要区分同一算法的多个实现时，用**语义化命名**（如 `_fast` / `_ref`、`_stage2_descent` / `_stage2_descent_aonly`、`train` / `train_aonly`），而不是版本号。
- 版本号只出现在**归档目录名 / 历史交付件 / CHANGELOG / 冻结产物**里，用于追溯，不进现役代码标识符。
